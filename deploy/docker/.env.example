# ============================================
# Smart Pocket Docker Environment Variables
# ============================================
# This is the PRIMARY configuration file for Docker development.
# Copy this file to .env and update with your actual values.
#
# To apply changes:
# docker-compose -f deploy/docker/docker-compose.dev.yml up -d --force-recreate

# OpenAI API Key (required for OCR parsing)
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4-turbo-preview

# Actual Budget Integration
# Get ACTUAL_BUDGET_SYNC_ID from: Settings → Advanced → Show advanced settings → Copy "Sync ID"
ACTUAL_BUDGET_PASSWORD=
ACTUAL_BUDGET_SYNC_ID=
ACTUAL_BUDGET_ENABLED=true

# API Key for mobile app connection (change in production)
API_KEY=dev_api_key_change_me

# JWT Secret (change in production)
JWT_SECRET=dev_jwt_secret_change_me

# Optional Features
GOOGLE_SHEETS_ENABLED=false

# Google Sheets Sync (Personal Feature - Optional)
# Uncomment to enable Google Sheets balance sync
# GOOGLE_SHEET_ID=your-spreadsheet-id-here
# GOOGLE_SHEET_NAME=Accounts
# GOOGLE_CREDENTIALS_JSON_PATH=/data/keys/smart-pocket-server.json  # Optional: defaults to this path

# Default Currency
DEFAULT_CURRENCY=USD

# Image Registry (for production deployment)
REGISTRY=localhost:5000
VERSION=latest
