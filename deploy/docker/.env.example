# ============================================
# Smart Pocket Docker Environment Variables
# ============================================
# Copy this file to create environment-specific configs:
#   - .env.dev  (for development)
#   - .env.qa   (for QA/staging)
#
# All environments use the same variables, just with different values.
# Production uses Docker secrets instead (see docker-compose.prod.yml)

# PostgreSQL Password
# Dev: Use simple password (e.g., dev_password)
# QA:  Generate with: openssl rand -hex 32
POSTGRES_PASSWORD=dev_password_change_me

# OpenAI API Key (required for OCR parsing)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4-turbo-preview

# Actual Budget Integration
# Password: Set this when you first open Actual Budget at http://localhost:5006 (dev) or :5007 (qa)
# Sync ID: Get from Actual Budget → Settings → Advanced → "Use end-to-end encryption" → Copy Sync ID
ACTUAL_BUDGET_PASSWORD=your_actual_budget_password
ACTUAL_BUDGET_SYNC_ID=your-sync-id-here
ACTUAL_BUDGET_ENABLED=true

# API Keys for Smart Pocket Server
# Dev: Use simple keys for convenience
# QA:  Generate secure keys with: openssl rand -hex 32
API_KEY=dev_api_key_change_me
JWT_SECRET=dev_jwt_secret_change_me

# Google Sheets Integration (optional - personal feature)
# Set to true to enable account balance syncing to Google Sheets
# Requires Google service account credentials at /keys/smart-pocket-server.json
GOOGLE_SHEETS_ENABLED=false
GOOGLE_SHEET_ID=your_google_sheet_id_here
GOOGLE_SHEET_NAME=Accounts

# Default Currency
DEFAULT_CURRENCY=USD
