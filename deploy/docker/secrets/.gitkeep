# Secrets directory
# Add your secret files here (they are gitignored)

# Required files for production:
# - postgres_password.txt
# - database_url.txt
# - openai_api_key.txt
# - api_key.txt
# - jwt_secret.txt

# Generate secrets with:
# openssl rand -hex 32 > api_key.txt
# openssl rand -hex 32 > jwt_secret.txt
